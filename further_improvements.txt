1. В файле xml_parser.py необходиом изменить абсолютный путь в FILE на относительный.(готово)
2. Написать функцию, которая при обновлении (при нажатии ПКМ -> Update по ячейке CFturbo Design) изменяла бы исходный
файл .cft-batch и сохраняла его.(готово)
    2.1. Написать промежуточную функцию, которая бы при обновлении брала одно (обновленное) значение ячейки свойств Tip Clearance
    и писала бы это новое значение в файл с названием output.xml. Перед этим понять как изменить текст в xml по тегу(атрибуту)(готово)

/*------- текущие задачи 26.02.18 --------*/

 - Добавить ещё один task ниже основного CFTurbo Design и назвать его СFTurbo Import
 - Создать еще один callback для создания файлов .curve и создания .inf из .tse

/*--------                        --------*/

3. При соединении системы ACT с TurboGrid должен меняться статус на "Update (Требуется обновление. Изменились входные данные
)" у TurboGrid. При нажатии refresh на ячейке TurboGrid должны создаваться следующие файлы shroud.curve, hub.curve, blade.curve и .tse (это файл сессии)
 .curve для hub, shroud и blade (profile) и .tse (это файл сессии), т.е. необходиом запускать Cfturbo в фоновом режиме.

4. Из TSE файла необходимо выделить информацию для формирования .inf-файла.